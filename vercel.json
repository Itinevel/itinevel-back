{
    "version": 2,
    "builds": [
      { "src": "./src/index.ts",
        "use": "@vercel/node",
        "config": { "includeFiles": ["src/**/*"] } }
    ],
    "rewrites": [
      { "source": "/(.*)", "destination": "/src" }
    ],
    "buildCommand": "npm run build"
  }
  
  