{
    "version": 2,
    "builds": [
      { "src": "/api/*.ts",
        "use": "@vercel/node"
       }
    ],
    
    "routes": [
      { "src": "/(.*)", "dest": "/api" }
    ],
    "buildCommand": "npm run build"
  }
  
  